/**
 * List of boolean "true" values
 */
export const trues: Array<string | number | boolean> = ["on", "true", "1", 1, true];

/**
 * List of boolean "false" values
 */
export const falses: Array<string | number | boolean> = ["off", "false", "0", 0, false];

/**
 * List of all boolean possible values
 */
export const booleans: Array<string | number | boolean> = [...trues, ...falses];
